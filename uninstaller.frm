//cleaning the framework
//removing all commands

alias framework "echo no framework installed..."
alias map(1)
alias map(2)
alias map(3)
alias map(4)
alias map(5)
alias map(6)
alias map(7)
alias map(8)
alias map(9)
alias all
secretmessage "You Found It"
alias scary(1)
alias scary(2)
alias cheats(1)
alias cheats(0)
alias uninstall()
alias reinstall()
alias health()
alias ammo()
alias hicon()
